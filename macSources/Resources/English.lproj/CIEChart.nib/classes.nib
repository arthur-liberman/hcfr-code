{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {displayModeChanged = id; showBlackBodyChanged = id; showGridChanged = id; }; 
            CLASS = HCFRCIEChartController; 
            LANGUAGE = ObjC; 
            OUTLETS = {optionsView = NSView; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}